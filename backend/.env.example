# Banco de dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=world_war

# Autenticacao
JWT_SECRET=troque_este_segredo_em_producao
JWT_EXPIRES=7d

# Servidor (o proxy do frontend aponta para esta porta)
PORT=3009
